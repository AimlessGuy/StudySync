<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:StudySync.Views"
       x:Class="StudySync.AppShell">

    <ShellContent Title="Home"
                  Route="MainPage"
                  ContentTemplate="{DataTemplate views:MainPage}" />

    <!-- Add these routes --><!--
    <ShellContent Title="Camera"
                  Route="CameraPage"
                  ContentTemplate="{DataTemplate views:CameraPage}" />

    <ShellContent Title="Vault"
                  Route="VaultPage"
                  ContentTemplate="{DataTemplate views:VaultPage}" />

    <ShellContent Title="Result"
                  Route="ResultPage"
                  ContentTemplate="{DataTemplate views:ResultPage}" />

    <ShellContent Title="NoteDetail"
                  Route="NoteDetailPage"
                  ContentTemplate="{DataTemplate views:NoteDetailPage}" />-->

</Shell>